# 画像および音声ファイルを読み込む際の注意点

画像ファイルは`/images`、音声ファイルは`/sounds`に保存してあり、各ステージ毎に分けられています

ファイルを読み込む際のパス指定は
```python

#画像読み込み例
pygame.image.load("images/stage(番号)/sample.png")

#音声読み込み例
pygame.mixer.music.load("images/stage(番号)/sample.wav")

```

となるようにお願いします。